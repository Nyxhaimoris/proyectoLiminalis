import AppRoutes from './routes';
import './App.css';

function App() {
  // Render the routing system (all pages/components are managed inside AppRoutes)
  return <AppRoutes />;
}

export default App;